import styled from "styled-components"
import { Image as ImageBoostrap } from "react-bootstrap";

export const Container = styled.main`
  flex: 1;
`;

export const Image = styled(ImageBoostrap)`
  width: 100%;
  height: 100%;
`;

export const Typography = styled.p`
  font-size: 1em;
`;